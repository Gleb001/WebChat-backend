// main ===================================================== //
const errorHandler = (error: Error) => console.log(error);

// epxort =================================================== //
export = errorHandler;